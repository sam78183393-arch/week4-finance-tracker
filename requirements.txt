# Personal Finance Tracker uses only the Python standard library.
# No external dependencies are required to run the application.
# This file is kept for future extensions (e.g. matplotlib for charts).
